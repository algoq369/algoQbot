═══════════════════════════════════════════════════════════════════════
  ✅ QUICK START: SHADOW MODE (AFTER SECURITY FIXES)
═══════════════════════════════════════════════════════════════════════

🎯 YOU ARE HERE: All 3 security fixes completed
📊 NEXT: Start 8-week shadow mode testing
⏱️  TIME: 30 minutes to start + 8 weeks monitoring

═══════════════════════════════════════════════════════════════════════
  STEP 1: VERIFY ENVIRONMENT (5 minutes)
═══════════════════════════════════════════════════════════════════════

cd /Users/sheirraza/bsc-ranging-bot

# 1. Check shadow mode is enabled
grep "SHADOW_MODE_ENABLED" .env
# Should say: SHADOW_MODE_ENABLED=true

# 2. Remove password from .env (SECURITY CRITICAL)
# Edit .env and delete or comment out this line:
# WALLET_PASSWORD=...

# 3. Verify encrypted wallet exists
ls -la wallet.json
# Should exist. If not: node scripts/setup-encrypted-wallet.js

═══════════════════════════════════════════════════════════════════════
  STEP 2: TEST STARTUP (10 minutes)
═══════════════════════════════════════════════════════════════════════

# Start bot with password prompt
node scripts/start-with-password.js

# You will see:
🔐 Enter wallet password: [type your password]
✅ Password accepted
👻 Shadow Mode: ENABLED (safe - no real trades)
🚀 Starting trading bot...
✅ Encrypted wallet loaded successfully
✅ PancakeSwap initialized with transaction verifier

# Let it run for 2-3 minutes to verify no errors

# Then press Ctrl+C to stop

# Check logs for errors
tail -50 logs/error.log
# Should be empty or only minor warnings

═══════════════════════════════════════════════════════════════════════
  STEP 3: START SHADOW MODE (5 minutes)
═══════════════════════════════════════════════════════════════════════

# Option A: Run in foreground (simple, but requires terminal open)
node scripts/start-with-password.js

# Option B: Run in background with nohup (recommended)
nohup node scripts/start-with-password.js > bot-output.log 2>&1 &

# Option C: Run in screen/tmux (best for remote servers)
screen -S trading-bot
node scripts/start-with-password.js
# Press Ctrl+A, then D to detach
# Reconnect with: screen -r trading-bot

═══════════════════════════════════════════════════════════════════════
  STEP 4: VERIFY IT'S RUNNING (5 minutes)
═══════════════════════════════════════════════════════════════════════

# Check if bot is running
ps aux | grep node

# View recent logs
tail -50 logs/combined.log

# Check shadow trades file is being created
ls -la .shadow-trades.json

# View shadow trades
cat .shadow-trades.json | jq '.metrics'

═══════════════════════════════════════════════════════════════════════
  MONITORING SCHEDULE (8 WEEKS)
═══════════════════════════════════════════════════════════════════════

WEEK 1 (Daily checks):
  - Check bot is running: ps aux | grep node
  - View errors: tail -50 logs/error.log
  - Count trades: cat .shadow-trades.json | jq '.trades | length'

WEEK 2-7 (2-3x per week):
  - Quick analysis: node scripts/analyze-shadow-results.js
  - Check logs: tail -100 logs/combined.log

WEEK 8 (Final analysis):
  - Comprehensive: node scripts/analyze-shadow-results.js
  - Review data: cat .shadow-trades.json | jq .

═══════════════════════════════════════════════════════════════════════
  AFTER 8 WEEKS: DECISION TIME
═══════════════════════════════════════════════════════════════════════

Run final analysis:
node scripts/analyze-shadow-results.js

IF IT SAYS "✅ READY FOR LIVE TRADING":
  1. Backup: cp .shadow-trades.json backup-$(date +%Y%m%d).json
  2. Edit .env: SHADOW_MODE_ENABLED=false
  3. Start: node scripts/start-with-password.js
  4. Use $25-50 ONLY
  5. Monitor DAILY
  6. Stop if 20% loss

IF IT SAYS "❌ NOT READY FOR LIVE TRADING":
  1. Stay in shadow mode
  2. Adjust strategy
  3. Run for another 4-8 weeks
  4. DO NOT trade with real money

═══════════════════════════════════════════════════════════════════════
  USEFUL COMMANDS
═══════════════════════════════════════════════════════════════════════

# Start bot
node scripts/start-with-password.js

# Check if running
ps aux | grep node

# Stop bot
pkill -f "node scripts/start-with-password.js"

# View logs
tail -f logs/combined.log         # Real-time
tail -100 logs/error.log           # Last 100 errors

# Analyze results
node scripts/analyze-shadow-results.js

# View shadow trades
cat .shadow-trades.json | jq .
cat .shadow-trades.json | jq '.metrics'
cat .shadow-trades.json | jq '.trades | length'

# Check rate limiter state
cat ratelimit-state.json

═══════════════════════════════════════════════════════════════════════
  TROUBLESHOOTING
═══════════════════════════════════════════════════════════════════════

Bot won't start:
  - Check: ls -la wallet.json (must exist)
  - Create: node scripts/setup-encrypted-wallet.js
  - Check: grep "SHADOW_MODE_ENABLED" .env (must be true)

Wrong password:
  - Re-enter password carefully
  - Check caps lock
  - Recreate wallet if forgotten: node scripts/setup-encrypted-wallet.js

Bot crashes:
  - View: tail -100 logs/error.log
  - Common issues:
    * RPC connection failed → Check internet
    * Out of gas → Check wallet balance
    * Rate limit hit → Normal, will resume in 1 hour

═══════════════════════════════════════════════════════════════════════
  CRITICAL REMINDERS
═══════════════════════════════════════════════════════════════════════

1. ⚠️  Remove WALLET_PASSWORD from .env NOW
2. ⚠️  Run shadow mode for FULL 8 weeks (no shortcuts)
3. ⚠️  Start live with $25-50 ONLY (not more)
4. ⚠️  Stop if 20% loss ($5-10 on $25-50)
5. ⚠️  Only scale up after 4+ weeks of PROVEN profit

═══════════════════════════════════════════════════════════════════════
  YOU'RE READY! START NOW:
═══════════════════════════════════════════════════════════════════════

node scripts/start-with-password.js

Good luck! 🚀🔒

═══════════════════════════════════════════════════════════════════════

